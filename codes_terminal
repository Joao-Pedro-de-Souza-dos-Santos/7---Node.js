------------------------------------------------------------------------------
CÓDIGOS npm PARA CRIAR/MONTAR/CONSTRUIR PROGRAMAS REACT/NODE 
 #INICIAR ARQUIVOS TS, INSTALAR BIBLIOTECAS :

npm init -y;

npm i typescript -D; 
    ou
npm i typescript tsx tsup -D;

npx tsc --init;

npm i express dotenv;
npm i --save-dev @types/express;

npm install zod;

npm i sqlite sqlite3;

npm i bcrypt;
npm i --save-dev @types/bcrypt;

npm i cookie-parse;

npm i dotenv;

npm i multer;
npm i --save-dev @types/multer

npm run build;

npm i vitest; 

npm start;

-------------------------------------------------------------------------------

# Adicionar valores a propriedade "script" [
    "dev": "tsx watch src/server.ts",
    "build": "tsup src",
    "start": "node dist/server.js"]
    em package.json;

-------------------------------------------------------------------------------

# Criar arquivo .gitignore e adicionar [
    node_modules
    dist
    .git
];

-------------------------------------------------------------------------------

# Criar arq .env e adicionar a PORT do server;

-------------------------------------------------------------------------------

# Criar pasta scr e add arq server.ts